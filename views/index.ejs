<%- include partials/header.ejs %>
<%- include partials/homenavbar.ejs %>
<body>
  <div class="container">
    <hr>
      <div class="jumbotron">
          <h1 class="display-4">Bem vido!</h1>
          <p class="lead">Este é um simples blog</p>
          <hr class="my-4">
          <p>Tenha todos os conteudo sobre programação.</p>
          <a class="btn btn-primary btn-lg" href="#" role="button">Leia mais</a>
        </div>
      <hr>
      <% articles.forEach(article => { %>
        <div class="card">
            <div class="card-header">
                <h2><%= article.title %></h2>
            </div>
            <div class="card-body">
              <a href="/<%= article.slug %>" class="btn btn-succes">Ler artigo</a>
            </div>

        </div>
     <% })%>
  </div>
</body>
<%- include partials/footer.ejs %>
